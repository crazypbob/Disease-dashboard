# 최신 명령어 (운영/재처리 메모)

작업이 끝나면 여기에 **최신으로 유효한 명령만** 남기고, 불필요해진 명령은 삭제합니다.

**상시 운영(메일→OCR→DB 자동)**: `docs/OPS-AUTOMATION.md` — `nas-auto-pipeline.py`를 **5분 등 스케줄에 등록**하는 것이 표준. crontab·Synology·Windows 작업 스케줄 예시는 동 문서 §5.

**Windows에서 `npx`가 막힐 때**: `docs/WINDOWS-EXECUTIONPOLICY.md` — 표준은 `npm run import:ocr -- ...`(또는 `tsx.cmd` 직접 실행).

---

## 2026-03-26

### NAS 작업 디렉터리 이동

```bash
cd /volume1/docker/질병메일링_대시보드/disease-dashboard
```

- **목적**: NAS 도커/파이프라인 작업을 `disease-dashboard` 폴더에서 수행

### (과거자료) 전체 재파싱 + DB 반영 (강제)

```bash
python3 scripts/nas-auto-pipeline.py --skip-imap --all-dates --reprocess-all
```

- **목적**: `.processed_pdfs.txt`/`ocr-pipeline/input`/`ocr-pipeline/output(results.xlsx)` 초기화 후, 전체 PDF를 다시 OCR → `results.xlsx` 재생성 → DB import(`--replace` 포함)

### (부분 재처리) input 폴더에 넣은 PDF만 OCR → DB 덮어쓰기

```bash
python3 scripts/nas-auto-pipeline.py --skip-imap --ocr-input-only --replace
```

- **목적**: 특정 PDF만 빠르게 재처리 (예: 조산 SIV, 성진 MH 등 일부 누락/오표기 수정)

### OCR 컨테이너 로그 확인

```bash
docker logs -f ocr-pipeline-ocr-pipeline
```

- **목적**: OCR 진행률/에러/완료 확인

---

## 2026-03-27

### (권장) DB 반영(덮어쓰기) — results.xlsx → DB

```bash
cd /volume1/docker/질병메일링_대시보드/disease-dashboard
docker run --rm --env-file env.nas -v "$(pwd)":/app -w /app node:20 sh -c "npm install --no-save @esbuild/linux-x64 && npx tsx scripts/import-ocr-results.ts --file=/app/ocr-pipeline/output/results.xlsx --replace"
```

- **목적**: OCR 결과(`results.xlsx`)를 DB에 **덮어쓰기 반영**(오표기/누락 수정 포함)
- **비고**: `--replace` 시 접수일자 통일 과정에서 남은 **stale 레코드(`pdf_file_id` null + 파일명날짜)**를 자동 정리하도록 되어 있음

### (부분 재처리) 특정 PDF만 빠르게 재OCR → DB 반영

```bash
cd /volume1/docker/질병메일링_대시보드/disease-dashboard
python3 scripts/nas-auto-pipeline.py --skip-imap --ocr-input-only --replace
```

- **목적**: `ocr-pipeline/input`에 넣은 PDF만 OCR하고 결과를 DB에 덮어쓰기 반영

---

## Windows PC에서 DB만 반영할 때 (로컬 경로)

```powershell
cd X:\질병메일링_대시보드\disease-dashboard
npm run import:ocr -- --file="X:/ocr-pipeline/output/results.xlsx" --replace
```

- **목적**: NAS Docker 없이 엑셀만 가져온 경우 등. `npx` 대신 `npm run` 사용(`docs/WINDOWS-EXECUTIONPOLICY.md`).

