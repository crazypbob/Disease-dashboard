# 개발일지 (질병메일링 대시보드)

## 그라운드룰

- **매일 정리할 때는 반드시 전날 일지를 먼저 읽고 시작**한다.
- 전날에 남긴 **진행 중 이슈/추적 관찰 항목**을 먼저 확인하고, 오늘 결과(해결/보류/추가조사)를 업데이트한다.
- “무엇을 했는지(What)”보다 **왜 했는지/판단 근거(Why)**와 **다음 액션(Next)**를 남긴다.

---

## 2026-04-24

### 오늘 한 일 (What / Why)

- **인증/가입승인/Drive 공유 운영 개선**: Google `AccessDenied`로 막히던 로그인 흐름을 “로그인은 허용하되 승인 전에는 대시보드/API 차단” 구조로 정리해, 사용자가 바로 가입 신청으로 유도되게 개선. 가입 승인 시 Drive 폴더(reader) 자동 공유 및 실패 재시도 화면(Drive 승인 탭) 추가. 네이버 OAuth 로그인도 추가하고, 네이버 사용자는 Drive 권한 부여용 Gmail(`drive_email`)을 별도 입력받도록 확장.
- **매트릭스 UI (`RecordsMatrix`)**: 스크롤 시 본문이 헤더에 겹쳐 보이던 문제를 **헤더용/데이터용 테이블 분리** + 동일 `colgroup`·`table-layout:fixed`·총폭(px)으로 **세로선 정렬**을 맞춤. **날짜(또는 주) 블록**마다 **굵은 왼쪽 테두리**로 구간 구분. 열 폭: **단일 검사 열 45px**, **PRRS 병합 75px**, **SIV·APP·MH 병합 열**은 전체 `records` 기준으로 Ag·Ab **슬롯이 둘 다 등장할 때만 75px**, 그렇지 않으면 45px (`lib/matrix.ts`의 `abAgMergedColumnHasBothSlotsInRecords`). 병합 셀 내부 Ag/Ab 라벨 간격은 `gap-2` 등으로 넓힘.
- **메일·운영 문서**: `docs/SETUP-NAVER-OCR.md`에 일별 `verify-imap-saves-for-day.py`·`IMAP_AUDIT_LOG`·**기본 자동 실행 아님 / 추후 스케줄**을 명시. `docs/00-DOCS-INDEX.md`, `docs/OCR-NAS-운영-정리.md` 상단에 정본 문서 포인터. `AGENTS.md`에 매트릭스·열 폭 규칙 반영.
- **배포 추적**: 변경 요약은 `docs/CHANGELOG.md` §「2026-04-24」에 기록.
- **OCR→DB import 성능 개선(장시간 재처리 단축)**: `scripts/import-ocr-results.ts`에 증분 옵션(`--since/--until`, `--only-missing`, `--only-tokens`)을 추가하고, 임시 테이블 기반 배치 업서트로 DB 왕복을 크게 줄이는 방향을 도입. 운영 파이프라인(`scripts/nas-auto-pipeline.py`)의 기본 import도 “최근 N일 + 누락분만”으로 바꿔 전체 재처리를 기본값에서 제거(환경변수 `IMPORT_DAYS_BACK`, `IMPORT_ONLY_TOKENS`로 제어).
- **(이슈 추적) 한빛청주(DB3023, ‘청주한빛’) 결과 미반영**: OCR+import 후에도 매트릭스에서 값이 비어 있다는 리포트. import 로그가 `삽입 0`인 경우 “DB에 신규 키가 들어오지 않았다”는 뜻이므로, 1) `results.xlsx`에 해당 농장/날짜 행 자체가 없거나(OCR 파서 단계), 2) 농장명이 매핑되지 않아 import에서 스킵되는 케이스를 우선 의심. `farm-mapping.ts`에는 `청주한빛/3023청주한빛/한빛청주 → DB3023` 매핑이 존재하므로, 실제 `results.xlsx`의 농장 셀 값이 무엇인지 확인이 필요.

### 다음 할 일 (Next)

- 브라우저에서 **주간/일간**·**가로 스크롤**까지 눈으로 한번 더 확인(서로 다른 열 폭 혼합 시).
- IMAP 일별 검증을 **운영에서 돌릴 경우** Task Scheduler·cron에 `verify-imap-saves-for-day.py` 등록(날짜 인자는 전날 등으로 치환).
- (DB3023 미반영) 아래 순서로 원인 확정:
  - `results.xlsx`에서 농장 열에 `한빛청주/청주한빛/3023청주한빛` 문자열이 있는지 검색(없으면 OCR 파서 단계 문제)
  - 있으면 import를 좁혀 재실행해 로그로 확인: `--only-tokens=DB3023` + `--since=2026-04-21` (기존값 덮어써야 하면 `--replace`)
  - `results.xlsx`의 농장 표기가 다른 별칭이라면 `lib/mail-pipeline/farm-mapping.ts`에 alias 추가

---

## 2026-04-28

### 오늘 한 일 (What / Why)

- **매트릭스 검증모드 강화**: `?verify=1`에서 **빈칸(레코드 누락)**, **링크없음(pdf 매칭 실패)**, **오표기(PDF 대조 후 수동 마킹)** 를 구분해 선택·전송 가능하게 확장. 링크없음 후보는 화면 범위 기준으로 **자동 수집/일괄 선택**을 제공. 검증모드에서 마우스 프리징이 심한 문제는 병합 셀 조회를 pair-map으로 최적화하고, 셀 체크박스 DOM을 기본 비활성화(토글)해 완화.\n+- **지도 탭: 기준점 반경 조회(ASF처럼)**: 주소 검색 또는 지도 빈 곳 클릭으로 기준점을 만들고, 기준점 주변 **500m·1·2·3·5km** 누적 목록으로 “타 농장 존재 여부”를 빠르게 확인할 수 있게 추가. 주소 정확도는 지오코더 한계가 있어 **기준점 마커 드래그로 미세 조정**을 지원.\n+- **지도 편의성**: 반경 원에 **라벨(500m/1km/2km…)을 지도 위에 항상 표시**하고, 라벨끼리·우측 패널과 겹치지 않도록 간단한 자동 회피 휴리스틱을 적용.\n+
### 다음 할 일 (Next)

- 지오코더는 추후 교체(VWorld/카카오 등)로 정확도 개선. 당장은 “검색→드래그 보정” UX 유지.\n+- 지도 라벨이 특정 줌/패널 조합에서 겹치면 후보 각도/금지영역(우측 패널 폭)을 조정.\n+
---

## 2026-04-25

### 오늘 한 일 (What / Why)

- **(원인 확정) 4/21 한빛청주(DB3023) 누락**: OCR+DB import를 해도 4/21 청주한빛(=한빛청주, DB3023) 행이 `results.xlsx`에 아예 없음을 확인. 저장 단계까지 추적한 결과 `naver-imap-to-nas.py` 기본 실행이 `UNSEEN`이라, **이미 읽음 처리된 메일의 첨부(PDF)가 저장되지 않아 누락**되는 케이스로 확정. `--all --since/--before`로 기간을 지정하면 즉시 복구됨(저장 로그로 확인).
- **(옵션3 확정) IMAP 감사로그 + 디스크 스캔을 “일별 집계”로 웹에서 확인**: 누락 여부를 빠르게 보려면 Vercel 웹이 NAS 경로에 접근할 수 없으므로, **NAS가 (1) `IMAP_AUDIT_LOG` JSONL + (2) `SAVE_PATH` 실제 파일 스캔**을 일별로 집계해 DB에 적재하고, 관리자 화면에서 mismatch를 강조 표시하는 방식으로 운영 방향을 고정.

### 다음 할 일 (Next)

- **누락 복구 표준 명령(읽음 포함 강제 다운로드)**:
  - `python3 scripts/naver-imap-to-nas.py --all --since=YYYY-MM-DD --before=YYYY-MM-DD`
- **(운영) `IMAP_AUDIT_LOG` 활성화 + 일별 집계 업로드 스케줄**:
  - NAS에서 `push-imap-daily-stats`를 매일 1회 실행해 `/dashboard/admin/imap-audit`에서 mismatch를 확인할 수 있게 구성
- **(주의) UNSEEN 기본 동작 유지 시**: “메일함에 있는데 파일이 없음”이 재발할 수 있으므로, 누락 발견 시 반드시 `--all --since/--before` 또는 compare 다운로드(`naver-nas-compare-download.py`)로 보완

## 2026-04-23

### 오늘 한 일 (What / Why)

- **Drive PDF → Google Drive 동기화 안정화**: `scripts/sync-pdfs-to-drive.ts`에 `--pending-only` 추가.
  - 목적: 502 등으로 끊겨도 **이미 갱신(Drive ID)된 건을 제외**하고, DB에 NAS 경로(`YYYY-MM/...pdf`)로 남은 것만 다시 처리.
  - 실행(실제 폴더 경로):

```powershell
npx.cmd tsx scripts/sync-pdfs-to-drive.ts --base="X:/질병메일링_대시보드/disease-dashboard/ocr-pipeline/input/검사결과_PDF" --pending-only
```

- **명령어 예시에 placeholder 경로 금지**: `.cursor/rules/real-paths-in-commands.mdc`(alwaysApply)로 “`X:/.../` 금지, `.env.local`의 `SAVE_PATH/PDF_BASE_PATH` 우선” 규칙을 고정.
- **홈(/) 로그인 UX 보강**: 로그인 이메일 표시 + `signOut({ callbackUrl: '/' })` 기반 로그아웃 버튼, `/auth/error`에도 “세션 끊기 후 재시도” 추가.
- **매트릭스 검증 오표기 정정(1건)**: `test_records.id=5633 (DB1003, 2026-04-21)`가 DB에 `PRRS/유전자분석/V`로 저장돼 있어 그대로 표시되던 것을 확인 후,
  - `세균/세균배양/음성`으로 DB 정정.
  - 보조 스크립트 추가: `scripts/inspect-test-record.ts`, `scripts/fix-record-5633-result.ts`, `scripts/fix-known-mislabels.ts` 업데이트.

### 다음 할 일 (Next)

- **(운영) Drive 연결 안 된 PDF 찾기**: DB에서 `pdf_file_id`가 아직 NAS 경로이거나 NULL인 레코드를 목록화해 월/농장/파일 단위로 재처리 대상 만들기.
- **(원인) OCR import 규칙 점검**: “유전자분석/분석결과/추출결과 → PRRS 유전자분석” 매핑이 세균성 텍스트 오타/누락 시 PRRS로 떨어지는 경로가 있는지 샘플링 후 수정.

---

## 2026-04-06

### 오늘 한 일 (What / Why)

- **항체가 추세 기능 방향 고정**: 사용 범위를 **농장주·담당 수의**의 **1농장 또는 2~3농장 비교**로 한정하고, 전체 매트릭스에 수치를 올리지 않음.
- **저장 채택**: **농장당 엑셀 1파일** + NAS 등에서 **`도/시군/면` 폴더**로 정리. **대시보드 런타임은 엑셀을 읽지 않고 DB만 조회** — 엑셀은 입력·백업·교환용, 정본은 import 후 Postgres.
- **문서화**: 상세 스펙·구현 단계(A~E)를 [`docs/TITER-TRACKING-SPEC.md`](docs/TITER-TRACKING-SPEC.md)에 기록. TODO·로드맵·문서 인덱스·CHANGELOG에 링크 반영.

### 다음 할 일 (Next)

- `TITER-TRACKING-SPEC` §5에 맞춰 **DDL 초안** 및 **엑셀 시트 헤더 스펙**(일령·산차·질병·날짜) 확정.
- Import 스크립트 스텁과 **담당 농장만 조회하는 API** 스코프를 `matrixScope`/`MATRIX_*` 와 정합.

---

## 2026-04-07

### 오늘 한 일 (What / Why)

- **로그인 주최 UX 정리(시연용)**: 상단 `로그인 주최`만으로 매트릭스/지도/좌측 농장선택이 같이 바뀌도록 단순화(뒤로가기/링크 포함).
- **농장주 데모 범위 축소**: 농장주 계정은 성진(DB1001)·대덕(DB1002)만 선택 가능하게 제한.
- **공수의 데모**: `pv(경기/충청)` 선택에 따라 public_vet 스코프로 매트릭스/지도를 제한(좌측 농장선택 없음).
- **MH 오표시 방지**: Optipharm 항원 결과서에서 *Mycoplasma hyorhinis*는 `MH`가 아니라 `MHR`로 분리(필터도 독립 노출).

### 내일 할 일 (Next)

- **네이버에서 3/25 이후 누락분 일괄 가져오기**(기간 지정 다운로드/비교) → NAS 저장 → OCR+import 재처리.
- **네이버 메일 도착 자동 감지→저장→OCR 자동 실행**을 상시 구동(서비스/스케줄러), 실패·중복 방지/로그 포함.

---

## 2026-04-05

### 오늘 한 일 (What / Why)

- **매트릭스 역할 뷰**: 정부(중앙 집계·익명 농장행 / 지방 관할 시·도), 공수의(경기·충청), 담당 수의, 수의+공수의 합집합 — 서버 `matrixScope`·`MATRIX_*_EMAILS`·`farm-locations` 주소로 스코프 강제, UI는 `DashboardContent` 6버튼.
- **지도 ASF 데모**: 전국 가상 30점, 점 클릭 시 500m~10km 동심원(색 구분)·우측 20% 거리 구간별 코드 목록, 제주 포함 맞춤·bounds 확장.
- **문서 일괄 정리**: 중복·구식 서술 제거를 위해 `docs/ROADMAP-PATENT-STATUS.md`를 특허·로드맵·완료/미완 단일 진입점으로 두고, `CHANGELOG`·`STATUS-SNAPSHOT`·`TODO`·`00-DOCS-INDEX`·`특허명세서_초안` 구현 표·`README`/`PROJECT-CONTEXT` 링크를 동기화.

### 다음 할 일 (Next)

- 지도 역할·가상 양성을 **실DB·서버 RBAC**와 맞출지 우선순위 확정 (`docs/TODO.md` P5).
- 임시출원용 **화면 캡처·도면**을 현재 UI(역할 6종, ASF 동심원)에 맞게 갱신.

---

## 2026-04-04

### 오늘 한 일 (What / Why)

- **지도 탭 역할·데이터 정리**: 정부(다비+행안부, 기준 클릭·반경·목록·CSV·패널), 공수의(경기/충청 주소+행안부 `sido`), 농장주 반경 클립, geo dedupe로 중복 마커·`<option>` key 경고 제거.
- **문서·특허 정합**: `CHANGELOG`·`TODO`·`STATUS-SNAPSHOT`·`PROJECT-CONTEXT`에 위 내용 반영; `특허명세서_초안.md`에 **구현 현황(완료/부분/미구현)** 표를 넣어 프로토타입과 명세의 차이를 내부 기록으로 고정.

### 다음 할 일 (Next)

- 지도 반경·양성을 **실DB**와 연동하고, 서버 RBAC과 지도 역할을 맞출지 우선순위 결정 (`docs/TODO.md` P5).

---

## 2026-03-26 (노트북)

### 오늘 한 일

- **PRRS 항체(ELISA) 판정 규칙 반영**
  - 기준: **S/P ≥ 0.4 양성**, **0.3 ≤ S/P < 0.4 의심(?)**, **S/P < 0.3 음성**
  - **여러 검체 중 하나라도 양성이면 전체를 양성**으로 집계
  - 전북대(jbnu) 파서에서 테이블/regex fallback 모두 숫자 기준으로 집계되도록 수정

- **화면 표기 확장**
  - 결과 표시 로직에 **`?`(의심)** 상태 추가
  - 매트릭스/리스트 뷰에서 `?`는 amber 색상으로 표시되도록 반영

- **PDF 링크 동작 수정 (Drive 미사용)**
  - 구형 Drive ID는 링크로 취급하지 않도록 변경
  - **NAS 경로(상대/절대 경로 형태)일 때만** `/api/pdf?id=...`로 열리도록 수정
  - 검증용 리포트 스크립트도 `/api/pdf` 기반으로 수정

- **성진(DB1001) 2026-03-24 누락( MH / APP ) 원인 및 수정**
  - 원인: 공통 `항원(PCR)` 파서가 PRRS/PED/TGE 위주로만 결과 컬럼 생성 → **`MH_결과`, `APP_결과`가 생성되지 않아 누락**
  - 조치: OCR 공통 파서에 `MH_결과`, `APP_결과` 추출 추가 + `import-ocr-results.ts` 컬럼 매핑/단일컬럼 파싱 패턴 추가

### 확인/관찰

- **성진(DB1001) 3/24~3/25 PRRS 항체 연속 양성**
  - 데이터/의뢰 중복 가능성 있음 → 재-import 후에도 동일 여부 및 원본 PDF 기준으로 재확인 필요

- **PowerShell 실행 정책 이슈로 npx 실행 실패**
  - 오류: `npx.ps1` 실행 차단 (ExecutionPolicy)
  - 운영 PC/환경별로 `npm/npx` 실행 방식(예: `node node_modules/.bin/tsx ...`)을 별도 가이드로 정리 필요

### 다음 할 일

- **(추적)** 성진 3/24~3/25 PRRS 항체 연속검사 사유/데이터 중복 여부 확인 (PDF, 접수번호, 파일명 기준)
- **(운영)** Windows 실행 정책 문제를 피하는 표준 실행 방법 문서화 (개발/운영용 명령 1개로 통일)

---

## 2026-03-27

### 오늘 한 일 (What / Why)

- **전북대(jbnu) 날짜 통일(중간/최종 중복 완화)**
  - 파일명 날짜(발송일) 대신 **PDF 본문 “접수일자”를 날짜로 사용**하도록 정리
  - 목적: 중간/최종이 서로 다른 날짜로 와도 **같은 검사(접수) 기준으로 매트릭스가 정리**되게

- **ELISA 판정 로직 개선**
  - **판정(양성/의심/음성) 텍스트를 우선**, 판정이 비어/미해독일 때만 S/P 숫자로 폴백
  - 폴백 발생 케이스는 DB에 `details=ELISA_JUDGEMENT_FALLBACK`로 기록하고 **관리자에서 목록 확인** 가능

- **전북대 ELISA 확장 (MH / 호흡기 ELISA)**
  - 파일명 기반으로 `MH ELISA` → `MH_항체`, `호흡기 ELISA` → `APP_항체 + MH_항체` 생성
  - `MH_항체`가 엑셀에 있어도 DB 반영이 안 되던 문제는 **import 매핑 누락을 추가**해 해결

- **전북대 항원(PCR) 타깃 컬럼 버그 수정**
  - `SIV PCR`, `PED PCR` 등이 **PRRS 결과 컬럼에 들어가던 문제**를 파일명/텍스트 기반으로 분기하여 해결

- **DB 오표시(양성 잔존) 정리**
  - “파일명 날짜”로 과거에 들어간 **stale 레코드(`pdf_file_id` null)** 때문에 매트릭스에 양성이 남아보이던 문제 해결
  - `--replace` import 시, `접수일자(date)`와 `파일명 날짜`가 다를 때 **stale 레코드를 자동 정리**하도록 추가

- **대시보드 운영/UX 정리**
  - 관리자 버튼: **`DB 새로고침` + `폴백 목록`만 유지** (혼란/오사용 가능 버튼 제거)
  - 매트릭스 헤더에서 `MH`가 **‘세균’으로 표시되던 버그** 수정 → `MH`로 표기

### 다음 할 일 (Next)

- `docs/COMMANDS.md`에서 레거시/중복 명령 정리하고, **실운영은 `최신명령어.md` 중심**으로 통일
- **(추후)** PDF 링크/경로 안정화: `pdf_file_id` 경로 의존성 제거(안정 키 기반 resolve)

