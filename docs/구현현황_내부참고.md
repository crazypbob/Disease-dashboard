# 구현 현황 (프로토타입 대비, 2026-04-05 기준)

> **내부 참고 문서** — 특허 명세서 첨부 불필요. 출원인이 코드베이스 상태를 명세 문구와 대조하기 위한 자료.
> 본 문서의 내용은 청구 범위를 스스로 제한하는 효과가 없으며, 정식출원 시 프로토타입과 발명 실시의 범위는 변리사와 정리한다.

> 제품 관점 **완료/미완·로드맵·추천 과제** 요약: [`ROADMAP-PATENT-STATUS.md`](ROADMAP-PATENT-STATUS.md)

---

## 표 1 — 명세 구성 요소(구성 1~7) 대비

| 구성 | 명세 요지 | 구현 상태 | 비고 |
|------|-----------|-----------|------|
| 1 매트릭스 3축 | 행·열·셀, Ag/Ab, PDF 링크 | **완료** | `RecordsMatrix.tsx`, `lib/matrix.ts`, `/api/pdf` |
| 2 복합 필터 | 질병·날짜·양성만 등 | **완료** | 6종 질병 필터·날짜 프리셋·양성만 표기 |
| 3 RBAC | 역할별 농장 범위 (6종 scope) | **완료** | `lib/matrix-region-filters.ts`·`DashboardContent.tsx` 역할 버튼 6종; 정부_중앙 집계 뷰(`SidoAggregateMatrix.tsx`) 포함. 공수의는 현재 도 단위 근사 → 상용화 시 시/군/구 세분화 |
| 4 반경·익명 | 코드만 공개, 반경 내 현황 | 부분 | 지도에서 DB코드·익명 표시; 인근 "양성" 표시는 **시드 기반 가상 양성 데모** (실제 DB 집계 미구현) |
| 5 파이프라인 | 검사결과 수신·파싱·DB | **완료** (운영 스케줄 이슈) | `scripts/nas-auto-pipeline.py`·`import-ocr-results.ts`·`drive-parse-to-results.ts` |
| 6 지도 | GPS·질병 레이어 | 부분 | 전국 농장(행안부 JSON) + 다비 실좌표 완료; 질병 필터 UI 완료; **ASF**: 전국 가상 30점·클릭 시 500m~10km 동심원·우측 거리구간 코드 목록은 **데모**(실DB 아님). PRRS 등 반경 내 주황 가상 양성도 데모. **매트릭스 DB 양성과 지도 실시간 연동**은 미구현 |
| 7 항체가 시계열 | 수치 필드·그래프 | **구현 예정** | DB 컬럼·그래프 컴포넌트 신설 예정 (항체가 추세 탭) |

---

## 표 2 — 실시예 1~6 대비

| 실시예 | 구현 상태 | 비고 |
|--------|-----------|------|
| 1 매트릭스 조회 | **완료** | 6종 역할 선택 버튼, gov_central 집계 뷰 포함 |
| 2 역할별 접근 | **완료** (근사) | 매트릭스 서버사이드 필터링 동작; 공수의는 도 단위 주소 규칙으로 근사 (시/군/구 자동 인식은 상용화 과제) |
| 3 반경 익명 현황 | 부분 | 반경 UI·지도 완료; 양성은 데모(mock); 익명 코드 체계는 DB코드 중심 |
| 4 자동 수집 파이프라인 | **완료** | 전자적 방법으로 수신·파싱·DB 저장 구현; 운영 자동화는 스케줄 등록 여부에 따름 |
| 5 지도 질병 시각화 | 부분 | 전국 농장 지도·질병 버튼 UI 완료; ASF·반경 양성 등 **시연용 데모**와 **DB 실시간 연동**은 미구현 |
| 6 항체가 시계열 | **구현 예정** | 항체가 추세 탭 신설 예정 |

---

## 표 3 — 청구항 대비 (요약)

| 청구항 | 대응 |
|--------|------|
| 1~3 | 매트릭스·PDF·Ag/Ab 서브컬럼 **완전 반영** |
| 4 | RBAC 6종 scope 서버사이드 구현 **완료**; 공수의 시/군/구 자동 인식은 상용화 보완 필요 |
| 5 | 반경 UI·익명 코드 완료; 인근 양성 DB 집계 **미구현** (데모) |
| 6 | 전국 지도·질병 필터·ASF 동심원 데모 등 UI 완료; **DB 기반 질병만 강조**·매트릭스-지도 실시간 연동 **미구현** |
| 7, 9 | 파이프라인·자동 갱신 **완료** (기술 중립적 구현) |
| 8, 10 | 항체가 시계열 모듈 **구현 예정** |
