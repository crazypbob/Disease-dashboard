# Python scripts (naver-vetdxlab-download 등)
# pip install -r requirements.txt
python-dotenv>=1.0.0
requests>=2.28.0
# vetdxlab HTML 뷰어 시 Playwright 폴백 (pip install playwright && playwright install chromium)
playwright>=1.40.0
